class User {
    constructor(name, email) {
      this.name = name;
      this.email = email;
    }
  
    save() {
      // save the user to the database
    }
  
    sendEmail() {
      // send an email to the user
    }
  }
  
  class User {
    constructor(name, email) {
      this.name = name;
      this.email = email;
    }
  
    save() {
      // save the user to the database
    }
  }
  
  class EmailService {
    sendEmail(user) {
      // send an email to the user
    }
  }
  